
#指定docker域名(分配随机IP)
hostname: ufire-redis
#指定服务运行的端口 映射6379到宿主机
ports:
  - "6379:6379"
ufire-getway:
  image: ufire-getway
  container_name: ufire-getway
  hostname: ufire-getway

ufire-websocket-1:
  #docker镜像名称
  image: ufire-websocket
  #docker容器名称
  container_name: ufire-websocket-1
  #指定docker域名(分配随机IP)
ufire-websocket-2:
  #docker镜像名称
  image: ufire-websocket
  #docker容器名称 随机分配IP
  container_name: ufire-websocket-2
ufire-websocket-3:
  #docker镜像名称
  image: ufire-websocket
  #docker容器名称 随机分配IP
  container_name: ufire-websocket-3
ufire-websocket-ui:
  #docker镜像名称
  image: ufire-websocket-ui
  #docker容器名称 随机分配IP
  container_name: ufire-websocket-ui
#ufire-elasticsearch:
#  image: elasticsearch:2.3
#  container_name: elasticsearch
#  hostname: ufire-elasticsearch
#  networks:
#    ufire-network:
#      ipv4_address: 172.19.0.111
#ufire-kibana:
#  image: kibana:4.5
#  container_name: kibana
#  links:
#    - ufire-elasticsearch:9200
#  ports:
#    - "5601:5601"
#ufire-logstash:
#  image: logstash:2.3
#  container_name: logstash
#  ports:
#    - "3456:3456"
#  volumes:
#    - root/logstash:/usr/share/logstash
#    - root/logstash/logstash.conf:/usr/share/logstash/config/logstash.conf
networks:
  ufire-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.19.0.0/16
